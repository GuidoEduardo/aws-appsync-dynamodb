import { User } from '../../model'
import { Team } from '../../model/team'
import { createTeamOptions, updateTeamMemberOptions } from './types'

export async function createTeam(options: createTeamOptions) {
  console.log("Fazendo")
}

export async function addMember(options: updateTeamMemberOptions) {
  console.log("Fazendo")
}

export async function changeLeader(options: updateTeamMemberOptions) {
  console.log("Fazendo")
}